<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.infection_bulletin" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->

        <!--
            Primary viene usato per:
            * Barra grande ( "Situazione generale" )
            * Sfondo pulsanti
            * Evidenziazione menu selezionato
            * Tasto conferma ( Ok / Ultimo disponibile / Selezionata nuova data )
        -->
        <item name="colorPrimary">@color/Primary</item>

        <!--
            PrimaryVariant viene usato per:
            * Barra piccola ( sopra barra grande )
         -->
        <item name="colorPrimaryVariant">@color/PrimaryVariant</item>

        <!--
            OnPrimary viene usato per:
            testo sopra Primary
         -->
        <item name="colorOnPrimary">@color/OnPrimary</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.infection_bulletin.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.infection_bulletin.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.infection_bulletin.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>